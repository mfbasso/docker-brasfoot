#!/usr/bin/env bash

set -euo pipefail

# Aguarda o display X estar disponível
export DISPLAY="${DISPLAY:-:1}"
timeout=60
until xset q &>/dev/null; do
	sleep 1
	timeout=$((timeout - 1))
	if [ "$timeout" -le 0 ]; then
		echo "Timeout aguardando display"
		exit 1
	fi
done
exec brasfoot
